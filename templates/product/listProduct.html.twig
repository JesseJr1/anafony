{% extends 'base.html.twig' %}

{% block title %}Nos Produits{% endblock %}

{% block body %}
<h1>Nos Produits</h1>   

<div class="container">
		{% for product in products %}

			<div class="col">
				<h2>{{ product.id}}</h2>
				<a href=" {{ path('app_product_detail', { id: product.id}) }} " >Voir le detail</a>
			{% endfor %}
			list product
		</div>
	</div>

{% endblock %}
